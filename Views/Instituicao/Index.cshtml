@model IEnumerable<AspNetCoreMVC.Models.Instituicao>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeInstituicao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndereçoInstituicao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeInstituicao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndereçoInstituicao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdInstituicao">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdInstituicao">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdInstituicao">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
